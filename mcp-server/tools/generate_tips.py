def generate_tips(session: dict, n: int) -> list[str]:
    """
    Implementierung: nutze Prompt, LLM-Routing (lokal/API).
    Rückgabe: Liste von n Tipps (strings).
    """
    # Platzhalter:
    return ["Tipp 1", "Tipp 2"]
