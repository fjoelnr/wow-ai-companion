version: "3.9"

services:
  ollama:
    image: ollama/ollama:latest
    container_name: ai-ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_KEEP_ALIVE=5m
    volumes:
      - ollama_models:/root/.ollama
    # GPU-Profile (genau eins aktivieren, sonst beide auskommentiert lassen):
    # NVIDIA:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - capabilities: ["gpu"]
    #           driver: "nvidia"
    # AMD (ROCm):
    # devices:
    #   - /dev/kfd
    #   - /dev/dri
    # group_add:
    #   - "video"

  mcp:
    build: ./mcp-server
    container_name: ai-mcp
    restart: unless-stopped
    environment:
      - LLM_MODE=local           # local|api
      - OLLAMA_BASE_URL=http://ollama:11434
      - OLLAMA_MODEL=llama3.2:3b
      - OLLAMA_FALLBACK_MODEL=
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=gpt-5-mini
    ports:
      - "9090:8080"
    volumes:
      - ./svshare:/svshare
    depends_on:
      - ollama

  watcher:
    build:
      context: ./external/python
      dockerfile: Dockerfile
    container_name: ai-watcher
    restart: unless-stopped
    environment:
      - SV_PATH=/wow/WTF/Account/103722624#1/SavedVariables/AICompanion.lua
      - SV_WRITE=/wow/WTF/Account/103722624#1/SavedVariables/AICompanion.lua
      - COMBATLOG_PATH=/wow/Logs/WoWCombatLog.txt
      - COMBATLOG_DIR=/wow/Logs
      - MCP_URL=http://mcp:8080
      - TIPS_COUNT=5
      - POLL_INTERVAL=2.0
    volumes:
      - "D:/Games/World of Warcraft/_retail_:/wow:ro"
      - "D:/Games/World of Warcraft/_retail_/WTF/Account/103722624#1/SavedVariables/AICompanion.lua:/wow/WTF/Account/103722624#1/SavedVariables/AICompanion.lua"
    depends_on:
      - mcp

  webui:
    build: ./webui
    container_name: ai-webui
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - mcp

volumes:
  ollama_models:
