# agents/AGENT.md

`agents/` contains Python agent modules (`coach`, `planner`, `retriever`, `validator`).

- Keep responsibilities separated by module.
- Favor deterministic interfaces between agents.
- Update prompts/logic with clear reasoning notes.
